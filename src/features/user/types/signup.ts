export enum EmailInputFieldError {
  REQUIRED = 'sign_up_form_email_required',
  INVALID = 'sign_up_form_email_invalid',
}

export enum FirstNameInputFieldError {
  REQUIRED = 'sign_up_form_first_name_required',
  MINIMUM = 'sign_up_form_first_name_minimum',
}

export enum LastNameInputFieldError {
  REQUIRED = 'sign_up_form_last_name_required',
  MINIMUM = 'sign_up_form_last_name_minimum',
}

export enum PasswordInputFieldError {
  REQUIRED = 'sign_up_form_password_required',
  INVALID = 'sign_up_form_password_invalid',
}
